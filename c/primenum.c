#include <studio.h>
#include <math.h>

int* a = malloc(10 * sizeof(int));
if(a==NULL){}

int isPrime(a) {
	int threshold = (int)(sqrt(a));
	for (int b    ) {
		flag = 1;
		if (a % b == 0) {
			flag = 0;
			return flag
		}
		if (b > threshold) {
			break;
		}
	}
	return flag
}